rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // WARNING: These rules allow anyone to read and write to your database.
    // This is required because the current app handles authentication on the client-side
    // and does not use Firebase Authentication.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}